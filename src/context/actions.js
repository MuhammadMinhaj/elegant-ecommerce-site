/* eslint-disable camelcase */
import types from './types';

const { } = types;

// eslint-disable-next-line no-unused-vars
const main = (dispatch, getState) => {
  const methods = {};
 
  return methods;
};
export default main;
